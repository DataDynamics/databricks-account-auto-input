setTimeout(() => {
  document.querySelector('div#login input[name=email]').removeAttribute('autocomplete');
  // document.querySelector('div#login input[name=email]').setAttribute('autocomplete', 'on');
}, 500);
